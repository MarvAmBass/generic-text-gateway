#!/bin/sh
# Create the service user; uucp group grants /dev/ttyUSB* access on Alpine.
addgroup -S gtg 2>/dev/null
adduser -S -D -H -h /var/lib/generic-text-gateway -s /sbin/nologin \
        -G gtg -g "generic-text-gateway" gtg 2>/dev/null
addgroup gtg uucp 2>/dev/null
exit 0
