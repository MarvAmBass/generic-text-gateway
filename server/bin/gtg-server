#!/usr/bin/env python3
"""Dev entry point — run from the repo without installing."""
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.abspath(__file__)), ".."))

from gtg_server.cli import main  # noqa: E402

sys.exit(main())
